# ╔════════════════════════════════════════════════════════════════╗
# ║        BubbleAnnounce Configuration v1.1.0                     ║
# ║  A highly configurable announcement plugin for Paper/Purpur    ║
# ╚════════════════════════════════════════════════════════════════╝

# ═══════════════════════════════════════════════════════════════════
# 📝 SUPER EASY GUIDE - READ THIS FIRST!
# ═══════════════════════════════════════════════════════════════════
# Want to ADD a new announcement?    → Copy any announcement below, change the name, edit the text
# Want to EDIT an announcement?      → Just change the text in "messages:"
# Want to REMOVE an announcement?    → Delete it or set "enabled: false"
# Made changes?                      → Type: /bubbleannounce reload
#
# That's it! Scroll down to see examples ⬇️
#
# ═══════════════════════════════════════════════════════════════════
# 💬 CUSTOM MESSAGES (Optional - ignore if you want defaults)
# ═══════════════════════════════════════════════════════════════════
# Fully customize all plugin messages and feedback
# Supports & color codes, hex colors (#RRGGBB), and placeholders
messages:
  # Command feedback messages
  no_permission: "&cYou do not have permission to use this command."
  announce_triggered: "&aAll announcements have been triggered!"
  invalid_argument: "&cInvalid argument! Use &e/test Announce &cto trigger all announcements."
  
  # Reload command messages
  reload_success: "&a✔ BubbleAnnounce configuration reloaded! &7({count} announcements)"
  reload_failed: "&c✖ Failed to reload configuration. Check console for errors."
  
  # List command messages
  list_header: "&b&l━━━━━━ Announcements ━━━━━━"
  list_footer: "&b&l━━━━━━━━━━━━━━━━━━━━━━━━"
  list_entry_enabled: "&7• &e{id} &a✔ Enabled &7({interval})"
  list_entry_disabled: "&7• &e{id} &c✖ Disabled &7({interval})"
  list_empty: "&eNo announcements configured."
  
  # Toggle command messages
  toggle_enabled: "&e{id} &7is now &aEnabled"
  toggle_disabled: "&e{id} &7is now &cDisabled"
  toggle_not_found: "&cAnnouncement not found: &e{id}"
  toggle_usage: "&cUsage: /bubbleannounce toggle <announcement_id>"
  
  # Test command messages
  test_triggered: "&aTriggered announcement: &e{id}"
  test_not_found: "&cAnnouncement not found: &e{id}"
  test_usage: "&cUsage: /bubbleannounce test <announcement_id>"
  
  # Debug command messages
  debug_enabled: "&eDebug mode: &aON"
  debug_disabled: "&eDebug mode: &cOFF"
  
  # Help menu messages
  help_header: "&b&l━━━━━ BubbleAnnounce v1.1.0 ━━━━━"
  help_footer: "&b&l━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  help_reload: "&e/bubbleannounce reload &7- Reload configuration"
  help_list: "&e/bubbleannounce list &7- List all announcements"
  help_toggle: "&e/bubbleannounce toggle <id> &7- Toggle announcement"
  help_test: "&e/bubbleannounce test <id> &7- Test specific announcement"
  help_debug: "&e/bubbleannounce debug &7- Toggle debug mode"
  
  # Plugin startup messages (console)
  startup_enabled: "BubbleAnnounce v1.1.0 enabled!"
  startup_loaded: "Loaded {count} announcements"
  startup_papi: "PlaceholderAPI integration enabled"
  startup_scheduled: "Scheduled announcement '{id}' every {interval} minutes"
  
  # Plugin shutdown messages (console)
  shutdown: "BubbleAnnounce disabled!"
  
  # Debug messages (console)
  debug_running: "Running announcement: {id}"
  debug_no_players: "No eligible players for announcement: {id}"
  debug_command_executed: "Executed command: {command}"
  debug_invalid_sound: "Invalid sound: {sound}"

# ═══════════════════════════════════════════════════════════════════
# ⚙️ SETTINGS (You probably don't need to change these)
# ═══════════════════════════════════════════════════════════════════
settings:
  prefix: ""            # Add text before every announcement (optional)
  debugMode: false      # Turn on for troubleshooting

# ═══════════════════════════════════════════════════════════════════
# 🎯 ANNOUNCEMENTS - Edit these!
# ═══════════════════════════════════════════════════════════════════
# COLOR CODES: &a=green &c=red &e=yellow &b=blue &6=gold &l=bold &f=white
# Copy any announcement below to make a new one!
#
announcements:
  
  # DISCORD - Join our Discord server (Copy this to make new announcements!)
  discord:
    enabled: true         # Turn on/off
    interval: 39          # Every 39 minutes
    messages:             # ← EDIT THE TEXT BELOW!
      - ""
      - "&9★ &fJoin our &9&lDiscord&f server!"
      - "&9★ &b&nhttps://discord.gg/mbP6FAw6zj"
      - ""

  # TICKET - Support tickets
  ticket:
    enabled: true
    interval: 27
    messages:
      - ""
      - "&c★ &fGot a problem? Make a ticket in Discord!"
      - ""

  # RESOURCE - Resource world reminder
  resource:
    enabled: true
    interval: 49
    messages:
      - ""
      - "&6★ &fGather resources in the &eResource World&f!"
      - "&6★ &fUse &b/rw &fto teleport there!"
      - ""

  # THANKS - Thank players (gives everyone 1 coin!)
  thanks:
    enabled: true
    interval: 30
    messages:
      - ""
      - "&6★ &fThanks for playing on &b&lBubblecraft&f!"
      - ""
    coinsGiveAll: true    # ← Everyone gets 1 coin when this sends!

  # BUG BOUNTY - Bug reports (OFF by default, manual only)
  bugbounty:
    enabled: false        # Change to true to enable
    interval: 0           # 0 = manual only (use /runannouncements)
    messages:
      - ""
      - "&c★ &fFound a bug? Report it in Discord for rewards!"
      - ""

  # VIP EXAMPLE - Shows advanced features (OFF by default)
  vip_example:
    enabled: false        # Set to true to test VIP features
    interval: 15
    messages:
      - "&6[VIP] &eVIP members get exclusive perks!"
    permission: "group.vip"           # Only players with this permission see it
    worlds: ["world"]                 # Only in specific worlds
    sound: "ENTITY_EXPERIENCE_ORB_PICKUP"  # Plays a sound
    cooldownSeconds: 300              # 5 minute cooldown per player

# ═══════════════════════════════════════════════════════════════════
# 📋 COPY THIS TO MAKE A NEW ANNOUNCEMENT!
# ═══════════════════════════════════════════════════════════════════
#  my_new_announcement:       # ← Change this name!
#    enabled: true            # Turn it on
#    interval: 20             # Every 20 minutes
#    messages:                # Your text here
#      - ""
#      - "&a★ Your message here!"
#      - ""

# ═══════════════════════════════════════════════════════════════════
# 🎮 COMMANDS
# ═══════════════════════════════════════════════════════════════════
# /bubbleannounce reload     - Reload after editing config
# /bubbleannounce list       - See all announcements
# /bubbleannounce toggle <id> - Turn announcement on/off
# /bubbleannounce test <id>  - Test an announcement
# /runannouncements          - Send all announcements now
#
# ═══════════════════════════════════════════════════════════════════
# 🎨 EXTRA: Advanced Settings (You probably don't need these!)
# ═══════════════════════════════════════════════════════════════════
# Want to add these to an announcement? Just add them below "messages:"
#
# mode: auto                 - Text format (auto/legacy/minimessage)
# type: chat                 - Display type (chat/actionbar/title)
# commands: []               - Run commands when announcement sends
# randomOrder: false         - Randomize message lines
# delayTicksBetweenLines: 0  - Delay between lines (20 = 1 second)
# permission: ""             - Only players with this permission see it
# worlds: []                 - Only show in specific worlds
# sound: ""                  - Play a sound (ENTITY_EXPERIENCE_ORB_PICKUP, etc.)
# prefix: ""                 - Custom prefix for this announcement
# cooldownSeconds: 0         - Cooldown before same player sees it again
